/**
 * Power
 * @param {number} m base
 * @param {number} n index
 * @returns {number}
 */
function pow (m, n) {
    // var result = m
    return alert(Math.pow(m, n));
}

var x = prompt('Введите число:');
var n = prompt('Введите степень:');

pow(x, n);
